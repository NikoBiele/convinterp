# Compile for the processor of this machine, including its fused multiply-add (FMA) instruction.
# Fine for local development and benchmarks; published wheels will need a portable baseline
# instead (a decision for later).
[build]
rustflags = ["-C", "target-cpu=native", "-C", "symbol-mangling-version=v0"]